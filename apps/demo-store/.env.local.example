# Publishable key for the seeded demo merchant — see supabase/seed_demo_merchant.sql
NEXT_PUBLIC_UPAY_API_KEY=

# Base URL of the UPay web app (apps/web) that hosts /api/v1/sessions and /checkout
NEXT_PUBLIC_UPAY_CHECKOUT_URL=http://localhost:3000
